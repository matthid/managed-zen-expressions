[package]
name = "zen-native"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "zen_native"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
